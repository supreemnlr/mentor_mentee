<p id="notice"><%= notice %></p>
<body bgcolor="cyan"></body>
<p>
  <b><h3>Category: </h3></b>
  <%= @category.name %>
</p>
<ul>
<h3> Subcategories: </h3>
<%@category.subcategories.each do |s|%>
   <li><%= s.name%></li>
<%end%>
</ul>

<%= link_to 'Edit', edit_admin_category_path(@admin_category) %> |
<%= link_to 'Back', admin_categories_path %>
<%= link_to 'Destroy', @admin_category, :confirm => 'Are you sure?', :method => :delete %></td>   
 
